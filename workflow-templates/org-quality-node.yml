# Node-focused org quality gate template.
name: Org Quality (Node)

on:
  pull_request:
  push:
    branches:
      - $default-branch
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality-gate:
    uses: antiphoria/.github/.github/workflows/quality-gate.yml@main
    with:
      policy-ref: main
      run-trivy: true
      run-python-ruff: false
      run-python-test: false
      run-python-codeaudit: false
      run-node-quality: true
      run-ci-handoff: true
